# PERLLON — Assistência Especializada Apple em Natal/RN
# robots.txt NÃO é mecanismo de segurança: o painel admin é protegido
# por autenticação + RLS no Supabase, independentemente deste arquivo.

User-agent: *
Allow: /

# Não indexar o painel administrativo (defesa em profundidade p/ SEO;
# o acesso continua bloqueado por Auth/RLS no servidor).
Disallow: /admin.html

# Apenas páginas com URLs próprias e indexáveis entram no sitemap.
Sitemap: https://perllon.com.br/sitemap.xml
